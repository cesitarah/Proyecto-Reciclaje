# Copia este archivo como .env en tu PC (no lo subas a GitHub).
# En Vercel, agrega la misma variable en Settings > Environment Variables.

DATABASE_URL="postgresql://usuario:password@host/neondb?sslmode=require"
